borderwidth 4
snapdist 64
gap 0 0 10 0
color activeborder turquoise
color inactiveborder slategray

# app specifications
ignore "xfce4-panel"
ignore "Global time"

# 4-Enter
command firefox firefox
command thunderbird thunderbird
command anki anki
command evince evince

#color activeborder lightskyblue
#color inactiveborder steelblue

unbind-key all
unbind-mouse all

bind-mouse 4-1 window-move
bind-mouse 4-2 window-resize
bind-mouse 4-3 window-resize

bind-key M-Tab    window-cycle
bind-key 4-Tab    window-cycle
bind-key MS-Tab   window-rcycle
bind-key 4-space      menu-window
bind-key 4-Escape menu-window
bind-key 4-Up      window-vmaximize
bind-key 4-Left      window-snap-left
bind-key 4-Right      window-snap-right
bind-key 4-b     window-vtile
bind-key 4-m      window-maximize
bind-key 4S-r           restart
bind-key 4-q           window-delete
bind-key 4-BackSpace           window-hide
bind-key 4S-q           quit

bind-key 4-1 group-only-1
bind-key 4-2 group-only-2
bind-key 4-3 group-only-3
bind-key 4-4 group-toggle-all
bind-key 4S-1 window-movetogroup-1
bind-key 4S-2 window-movetogroup-2
bind-key 4S-3 window-movetogroup-3
